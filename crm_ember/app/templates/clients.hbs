<div class="row">
  <div class="col-md-2">
    {{#link-to 'clients.new'}}New{{/link-to}}
    <ul class="list-group">
      <a href="#" class="list-group-item active"> Clients  </a>
      {{#each client in controller}}
        <li class="list-group-item">
          {{#link-to 'client' client }} {{ client.name }} {{/link-to}}
        </li>
      {{/each}}
    </ul>
  </div>

  <div class="col-md-10">
    {{outlet}}
  </div>
</div>