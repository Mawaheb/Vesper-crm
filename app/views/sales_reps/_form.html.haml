<!-- = simple_form_for @sales_rep do |f| -->
- if @sales_rep.errors.any?
  #error_explanation
    %h2
      = pluralize(@sales_rep.errors.count, "error")
      prohibited this sales_rep from being saved:
    %ul
      - @sales_rep.errors.full_messages.each do |msg|
        %li= msg
= f.input :first_name
= f.input :last_name
= f.input :title
= f.input :phone_num
= f.input :email
= f.label :sex
= f.select :sex, Person::SEX
.actions
  = f.submit